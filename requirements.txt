opencv-python>=4.9.0.80
diffusers>=0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.1.1
tqdm
imageio
easydict
ftfy
dashscope
imageio-ffmpeg
scikit-image
loguru
gradio>=5.0.0
numpy>=1.23.5,<2
einops
librosa
soundfile
safetensors
scipy
# torchvision  -> backend-specific pin required (must match torch ABI).
#                  CUDA host: pip install torchvision (let resolver match).
#                  NPU host: torchvision==0.22.1 pinned in requirements-npu.txt
#                  to match torch==2.7.1 / torch_npu==2.7.1.post6 ABI.
Pillow
# xfuser>=0.4.1  -> moved to requirements-cuda.txt; CUDA-only multi-card SP path.
#                   On NPU host (single-card MVP per Story 1.3 short-circuit) it's
#                   unreachable at runtime, and its CUDA build deps fail to install
#                   on aarch64 NPU hosts. See README-NPU.md (Story 1.7) and
#                   requirements-cuda.txt.
pyloudnorm
optimum-quanto==0.2.6
scenedetect
moviepy==1.0.3
# decord  -> backend-specific install required on aarch64 NPU hosts.
#            * CUDA host: pip install decord (PyPI wheel works).
#            * NPU/aarch64 host: source-build (eva-decord lacks cp310-aarch64
#              wheels as of 2026-04). See README-NPU.md (Story 1.7).
